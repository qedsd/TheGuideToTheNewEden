<Page
    x:Class="TheGuideToTheNewEden.WinUI.Views.GamePreviewMgrPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:CommunityToolkit.WinUI.UI.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:TheGuideToTheNewEden.WinUI.Views"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:model="using:TheGuideToTheNewEden.WinUI.Helpers"
    xmlns:viewmodels="using:TheGuideToTheNewEden.WinUI.ViewModels"
    Background="Transparent"
    mc:Ignorable="d">
    <Page.DataContext>
        <viewmodels:GamePreviewMgrViewModel x:Name="VM" />
    </Page.DataContext>
    <Grid Margin="20">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="300" />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <TextBox
                Header="窗口关键词"
                PlaceholderText="多个关键词时，使用符号,分割"
                Text="{x:Bind VM.Setting.TitleKeywords, Mode=TwoWay}" />
            <ListView
                Grid.Row="1"
                Background="Transparent"
                ItemsSource="{x:Bind VM.WindowInfos, Mode=OneWay}"
                SelectedItem="{x:Bind VM.SelectedWindowInfo, Mode=TwoWay}">
                <ListView.ItemTemplate>
                    <DataTemplate x:DataType="model:WindowInfo">
                        <TextBlock Text="{x:Bind Title}" ToolTipService.ToolTip="{x:Bind Title}" />
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
            <Button
                Grid.Row="2"
                HorizontalAlignment="Center"
                Command="{x:Bind VM.RefreshWindowCommand}"
                Content="更新列表" />
        </Grid>
        <Grid Grid.Column="1" Background="Transparent">
            <Grid.RowDefinitions>
                <RowDefinition />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <!--<controls:ImageCropper
                x:Name="PreviewImage"
                Background="Transparent"
                CropShape="Rectangular"
                ThumbPlacement="All" />-->
            <Image Source="{x:Bind VM.PreviewImage, Mode=OneWay}" />
        </Grid>
    </Grid>
</Page>
