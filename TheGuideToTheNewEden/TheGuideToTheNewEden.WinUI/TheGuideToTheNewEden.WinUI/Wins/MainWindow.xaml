<Window
    x:Class="TheGuideToTheNewEden.WinUI.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:ic="using:FluentIcons.WinUI"
    xmlns:local="using:TheGuideToTheNewEden.WinUI"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:views="using:TheGuideToTheNewEden.WinUI.Views"
    mc:Ignorable="d">

    <Grid x:Name="MainWindowGrid">
        <Grid x:Name="BackgroundGrid" Visibility="Collapsed">
            <Grid.Background>
                <ImageBrush
                    x:Name="BackgroundImage"
                    Opacity="1"
                    Stretch="UniformToFill" />
            </Grid.Background>
            <Grid>
                <Grid.Background>
                    <SolidColorBrush x:Name="BackgroundBrush" Color="Transparent" />
                </Grid.Background>
            </Grid>
        </Grid>
        <views:MainPage />
        <Grid
            x:Name="AppTitleBarGrid"
            Height="32"
            Margin="0,0,136,0"
            VerticalAlignment="Top"
            Canvas.ZIndex="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <Border
                x:Name="AppTitleBar"
                Grid.Column="0"
                VerticalAlignment="Stretch" />
            <Button
                x:Name="TopButton"
                Grid.Column="1"
                VerticalAlignment="Stretch"
                Click="Button_Top_Click"
                Style="{StaticResource TitleBarTopButtonStyle}" />
        </Grid>
    </Grid>
</Window>
