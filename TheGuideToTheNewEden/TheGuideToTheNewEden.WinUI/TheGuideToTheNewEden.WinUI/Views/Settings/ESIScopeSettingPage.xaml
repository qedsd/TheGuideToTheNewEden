<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="TheGuideToTheNewEden.WinUI.Views.Settings.ESIScopeSettingPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:TheGuideToTheNewEden.WinUI.Views.Settings"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:setting="using:TheGuideToTheNewEden.WinUI.ViewModels.Setting"
    Background="Transparent"
    NavigationCacheMode="Required"
    mc:Ignorable="d">
    <Page.DataContext>
        <setting:ESIScopeSettingViewModel x:Name="VM" />
    </Page.DataContext>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition />
        </Grid.RowDefinitions>
        <Grid Margin="16,8">
            <CheckBox Content="{StaticResource ESIScopeSettingPage_SelectAll}" IsChecked="{x:Bind VM.SelectAll, Mode=TwoWay}" />
        </Grid>
        <Rectangle
            Grid.Row="1"
            Height="1"
            Margin="8,0"
            Fill="Gray" />
        <ListView
            Grid.Row="2"
            Margin="0,8,0,0"
            ItemsSource="{x:Bind VM.Scopes, Mode=OneWay}">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <CheckBox Content="{Binding Scope}" IsChecked="{Binding Selected, Mode=TwoWay}" />
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>
