<UserControl
    x:Class="TheGuideToTheNewEden.WPF.Views.UserControls.MarketTypeInfoView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Background="Transparent">

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!--  物品名称 + 图标  -->
        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Image
                Width="48"
                Height="48"
                VerticalAlignment="Top"
                Source="{Binding SelectedInvTypeIcon}" />
            <TextBlock
                Grid.Column="1"
                Margin="12,0,0,0"
                VerticalAlignment="Center"
                FontSize="18"
                FontWeight="SemiBold"
                Foreground="{DynamicResource TextFillColorPrimaryBrush}"
                Text="{Binding SelectedInvTypeName}"
                TextWrapping="Wrap" />
        </Grid>

        <!--  体积  -->
        <StackPanel
            Grid.Row="1"
            Margin="0,12,0,0"
            Orientation="Horizontal">
            <TextBlock
                FontSize="12"
                Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                Text="{DynamicResource MarketPage_Volume}" />
            <TextBlock
                Margin="6,0,0,0"
                Foreground="{DynamicResource TextFillColorPrimaryBrush}"
                Text="{Binding SelectedInvTypeVolumeText}" />
        </StackPanel>

        <Border
            Grid.Row="2"
            Height="1"
            Margin="0,12,0,0"
            Background="{DynamicResource DividerStrokeColorDefaultBrush}" />

        <!--  简介：后续在此扩展 SDE 更详细属性  -->
        <ScrollViewer
            Grid.Row="3"
            Margin="0,12,0,0"
            HorizontalScrollBarVisibility="Disabled"
            VerticalScrollBarVisibility="Auto">
            <TextBlock
                Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                Text="{Binding SelectedInvTypeDescription}"
                TextWrapping="Wrap" />
        </ScrollViewer>
    </Grid>
</UserControl>
